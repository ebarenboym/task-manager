# Ellie's Focus Board

A custom task management application built with Next.js, React, and Tailwind CSS for Module 10.

## Setup

To view the project:

- Run `npm install` to install the required packages.
- Run `npm run dev` to start the local development server.
- Open `http://localhost:3000` in your browser.

## AI Usage Log

## AI Usage Log

### Entry 1
I used ChatGPT to help debug an issue with localStorage where my tasks were disappearing after refreshing the page. It explained why the issue was happening in Next.js, helped me update the code, and verified that the tasks were saving and loading correctly.

### Entry 2
I used ChatGPT to troubleshoot a hydration error that appeared after changing how localStorage was loaded. It explained what the error meant, helped me move the localStorage logic into the correct place, and confirmed that the application was working again.

### Entry 3
I used ChatGPT to get feedback on the visual. I want the pink deisgn to be more cohessive, so I used AI to help me design it better. 